Host *
  Protocol 2
  ConnectTimeout 30
  ServerAliveInterval 15
  ProxyCommand nc -X connect -x ${SSH_HTTP_PROXY} %h %p
  ControlMaster auto
  ControlPath ~/.ssh/sockets/%r@%h:%p
  ControlPersist 60s
